import React from 'react';
import ListView from './ListView';

const OrderList = () => {
    return (
        <div>
            <div>
                <ListView />
            </div>
        </div>
    );
}

export default OrderList;
